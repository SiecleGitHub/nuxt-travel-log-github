# Nuxt Travel Log

A full stack application built with Nuxt that allows users to keep track of all the places they've been.

## With a lot of help from a powerful and kind AI agent

🚀 Your current setup is now genuinely healthy

You now have a pretty modern stack working together correctly:

Nuxt 4
pnpm 9
ESLint 10
flat config
@antfu/eslint-config
Vercel
TypeScript
Nitro

That combination is powerful, but the initial setup phase is unfortunately rough these days.

## Setup

Make sure to install dependencies:

```bash
pnpm install
```

## Development Server

Start the development server on `http://localhost:3000`:

```bash
pnpm dev
```

## Production

Build the application for production:

```bash
pnpm build
```

Locally preview production build:

```bash
pnpm preview
```
